<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ИИ с человеческим самосознанием</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', sans-serif;
        }

        body {
            background: #1a1a1a;
            color: #e0e0e0;
            line-height: 1.6;
        }

        header {
            background: #2d2d2d;
            padding: 2rem;
            text-align: center;
            border-bottom: 3px solid #4CAF50;
        }

        nav {
            background: #333;
            padding: 1rem;
            text-align: center;
        }

        nav a {
            color: #4CAF50;
            text-decoration: none;
            margin: 0 1rem;
            font-weight: bold;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 2rem;
        }

        .section {
            background: #262626;
            border-radius: 10px;
            padding: 2rem;
            margin: 2rem 0;
            box-shadow: 0 4px 6px rgba(0,0,0,0.3);
        }

        h1, h2 {
            color: #4CAF50;
            margin-bottom: 1.5rem;
        }

        .code-block {
            background: #333;
            padding: 1rem;
            border-radius: 5px;
            margin: 1rem 0;
            overflow-x: auto;
        }

        pre {
            white-space: pre-wrap;
            font-family: 'Courier New', monospace;
            color: #9CCC65;
        }

        footer {
            background: #2d2d2d;
            text-align: center;
            padding: 1rem;
            margin-top: 2rem;
        }

        .document-view {
            width: 100%;
            height: 600px;
            border: none;
            background: white;
        }
    </style>
</head>
<body>
    <header>
        <h1>Проект: ИИ с человеческим самосознанием</h1>
        <p>Автор: [Ваше имя]</p>
    </header>

    <nav>
        <a href="#document">Документ</a>
        <a href="#code">Код</a>
        <a href="#contact">Контакты</a>
    </nav>

    <div class="container">
        <section id="document" class="section">
            <h2>Документация</h2>
            <embed class="document-view" src="your-document.pdf" type="application/pdf">
        </section>

        <section id="code" class="section">
            <h2>Примеры кода</h2>
            
            <div class="code-block">
                <h3>QETLoss (Keras)</h3>
                <pre><code>
class QETLoss(tf.keras.losses.Loss):
    def __init__(self, alpha=0.3, beta=0.2, gamma=0.1, delta=0.25, epsilon=0.15):
        super().__init__()
        self.alpha = alpha  # Вес креативности
        self.beta = beta    # Вес этики
        ...

    def call(self, y_true, y_pred):
        mse = tf.reduce_mean(tf.square(y_true - y_pred))
        creativity_loss = self.alpha * (1 - tf.math.abs(y_pred))
        return mse + creativity_loss - ethics_loss
                </code></pre>
            </div>

            <div class="code-block">
                <h3>Триумвиратная архитектура</h3>
                <pre><code>
inputs = tf.keras.Input(shape=(100,))
x = Dense(64, activation='relu')(inputs)
outputs = Dense(10, activation='softmax')(x)
model = Model(inputs, outputs)
model.compile(optimizer='adam', loss=QETLoss())
                </code></pre>
            </div>
        </section>

        <section id="contact" class="section">
            <h2>Контакты</h2>
            <form>
                <input type="text" placeholder="Ваше имя" required>
                <input type="email" placeholder="Ваш Email" required>
                <textarea placeholder="Сообщение..." rows="5"></textarea>
                <button type="submit">Отправить</button>
            </form>
        </section>
    </div>

    <footer>
        <p>© 2024 Проект "ИИ с человеческим самосознанием". Все права защищены.</p>
        <p>GitHub: <a href="[Ваш репозиторий]" style="color: #4CAF50;">[Ссылка]</a></p>
    </footer>
</body>
</html>
